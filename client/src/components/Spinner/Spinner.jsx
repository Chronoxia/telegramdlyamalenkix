import React from 'react';
import './Spinner.scss';

const Spinner = () => (
    <div className="spinner">
        <i className="fa fa-spinner fa-spin" style={{fontSize:"90px", color: "white", margin: "auto"}}></i>
    </div>
);

export default Spinner;
